# Backend API URL — set this in Netlify environment variables
# For local dev: leave blank (uses vite proxy)
# For production: set to your backend URL e.g. https://blogverse-api.onrender.com/api
VITE_API_URL=
